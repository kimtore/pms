0.43:
- Tag browser: genre, album, artist, etc.
- Tab completion
- Config system rewrite (bind, set accepted in the same function)
- Command-line interface to filters
- Idle support
- Separate threads (timing, interface)

Future:
- better album detection, and also customizable through the 'albumclass' setting.
- Display rewrite
